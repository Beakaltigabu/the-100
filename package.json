{
  "name": "the-100",
  "version": "1.0.0",
  "private": true,
  "description": "THE 100 — 100 days. Your goal. Your commitment.",
  "scripts": {
    "db:setup": "node db/setup.js",
    "db:migrate": "npm --prefix db run migrate",
    "db:seed": "npm --prefix db run seed",
    "db:reset": "npm run db:setup && npm run db:migrate && npm run db:seed",
    "dev": "concurrently -n server,client -c yellow,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "test": "npm --prefix server run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}